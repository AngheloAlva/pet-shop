import React from 'react'

interface ProductCardProps {
  products: any[]
}

const ProductCard = ({ products }: ProductCardProps): JSX.Element => {
  return (
    <div>ProductCard</div>
  )
}

export default ProductCard
